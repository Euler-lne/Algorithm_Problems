#include <iostream>
#include <string>
// 树状数组
using namespace std;

void is_palindrome(string s)
{
}

int main()
{
    int n;
    for (int i = 0; i < n; i++) {
        string s;
        cin >> s;
        is_palindrome(s);
    }
    return 0;
}