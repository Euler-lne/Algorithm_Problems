#include <iostream>

#define MAX 105

using namespace std;

int price[MAX][MAX], b[MAX][MAX], mi[MAX];
int n;

int main()
{
    ios::sync_with_stdio(false);
    int t;
    cin >> t;
    while (t--) {
        cin >> n;
        for (int i = 1; i <= n; i++) {
            cin >> mi[i];
            for (int j = 1; j <= mi[i]; j++) {
            }
        }
    }
    return 0;
}